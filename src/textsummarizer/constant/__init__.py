from pathlib import Path

# YAML file paths
CONFIG_PATH = Path("config\config.yaml")
PARAMS_PATH = Path("params.yaml")

# Function
def add(a, b):
    return a + b

# Explicitly define what should be available when imported
